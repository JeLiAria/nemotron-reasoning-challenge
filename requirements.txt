# NVIDIA Nemotron Model Reasoning Challenge — dependances directes
# Pipeline d'entrainement du run 0.85 (LoRA rank-32, backend Tinker).
# Versions EXACTES issues de uv.lock (le lock complet est dans requirements-lock.txt).
# Python 3.11
#
#   python -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt
#
# Note : le notebook Kaggle (notebook/kaggle_writeup_notebook.ipynb) n'a AUCUNE
# dependance externe — stdlib uniquement. Voir notebook/INSTALL.md.

tinker==0.22.3
tinker-cookbook==0.3.0
torch==2.11.0
transformers==4.57.6
jinja2==3.1.6
modal==1.4.1
pydantic==2.12.1
